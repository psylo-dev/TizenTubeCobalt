name: xbox ci

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: "Build Type (gold oder debug)"
        required: true
        default: "gold"
        type: choice
        options:
          - gold
          - debug

      upload_release:
        description: "Upload als Release Asset"
        required: false
        default: false
        type: boolean

jobs:
  xb1:
    runs-on: windows-latest

    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # -------------------------
      # CACHE LAYER (Speed Boost)
      # -------------------------
      - name: Cache Pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-${{ hashFiles('requirements.txt') }}

      - name: Cache GN
        uses: actions/cache@v4
        with:
          path: C:\tools\gn
          key: gn-windows

      # -------------------------
      # PYTHON (fast install)
      # -------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Python deps
        shell: pwsh
        run: pip install -r requirements.txt --prefer-binary --no-input

      # -------------------------
      # MSVC ENV (NO INSTALLS)
      # -------------------------
      - name: Setup MSVC Environment
        shell: pwsh
        run: |
          $vsWhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vsPath = & $vsWhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath

          $vcvars = "$vsPath\VC\Auxiliary\Build\vcvars64.bat"
          $envOut = cmd /c "`"$vcvars`" && set"

          foreach ($line in $envOut) {
            if ($line -match "^([^=]+)=(.*)$") {
              Add-Content $env:GITHUB_ENV "$($matches[1])=$($matches[2])"
            }
          }

      # -------------------------
      # TOOLS (GN + NASM)
      # -------------------------
      - name: Install Tools
        shell: pwsh
        run: |
          if (!(Test-Path "C:\tools\gn")) {
            New-Item -ItemType Directory -Force C:\tools\gn | Out-Null
            Invoke-WebRequest "https://chrome-infra-packages.appspot.com/dl/gn/gn/windows-amd64/+/latest" -OutFile gn.zip
            Expand-Archive gn.zip C:\tools\gn -Force
          }

          if (!(Get-Command nasm -ErrorAction SilentlyContinue)) {
            choco install nasm -y --no-progress
          }

      - name: Add GN to PATH
        shell: pwsh
        run: Add-Content $env:GITHUB_PATH "C:\tools\gn"

      # -------------------------
      # BUILD CONFIG
      # -------------------------
      - name: Set Build Variables
        shell: pwsh
        run: |
          Add-Content $env:GITHUB_ENV "BUILD_TYPE=${{ inputs.build_type }}"
          Add-Content $env:GITHUB_ENV "OUTPUT_DIR=out/xb1_${{ inputs.build_type }}"

      # -------------------------
      # RESOURCES (fast)
      # -------------------------
      - name: Download Resources
        shell: pwsh
        run: python download_resources.py

      # -------------------------
      # GN + BUILD (FAST MODE)
      # -------------------------
      - name: GN Gen
        shell: pwsh
        run: python cobalt/build/gn.py -p xb1 -c ${{ inputs.build_type }} -o ${{ env.OUTPUT_DIR }}

      - name: Build (NINJA FAST)
        shell: pwsh
        run: |
          $env:NINJA_STATUS='[%e sec | %f/%t | %r]'
          ninja -C ${{ env.OUTPUT_DIR }} cobalt_install -j $env:NUMBER_OF_PROCESSORS

      # -------------------------
      # PACKAGE
      # -------------------------
      - name: Package Appx
        shell: pwsh
        run: python starboard/xb1/tools/packager.py -s ${{ env.OUTPUT_DIR }}/ -o ${{ env.OUTPUT_DIR }}/package -p cobalt

      - name: Verify Appx
        shell: pwsh
        run: |
          $appx = Get-ChildItem "${{ env.OUTPUT_DIR }}/package" -Filter "*.appx"
          if (!$appx) { throw "APPX nicht gefunden" }
          Write-Host "OK: $($appx.FullName)"

      # -------------------------
      # UPLOAD
      # -------------------------
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cobalt-xb1-${{ inputs.build_type }}
          path: |
            ${{ env.OUTPUT_DIR }}/cobalt*
            ${{ env.OUTPUT_DIR }}/package/**

      - name: Release Upload
        if: inputs.upload_release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ${{ env.OUTPUT_DIR }}/package/*.appx
          file_glob: true
          asset_name: cobalt-xb1-${{ inputs.build_type }}.appx
          tag: manual-build
          overwrite: true
